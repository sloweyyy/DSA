 for (auto it = s.begin(); it != s.end(); it++)
    {
        if (s.find(1) != s.end())
        {
            idx = *it;
            cout << "Thu tu cua 1 la: " << idx << endl;
            break;
        }
    }